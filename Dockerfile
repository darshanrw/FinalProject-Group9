ARG BASE_IMAGE
FROM ${BASE_IMAGE}

ARG DEPENDENCIES
ARG INSTALL_COMMAND
ARG BUILD_COMMAND
ARG WORK_DIR

WORKDIR ${WORK_DIR}

COPY ${DEPENDENCIES} ${WORK_DIR}/

RUN ${INSTALL_COMMAND}

COPY . ${WORK_DIR}/

EXPOSE ${EXPOSE_PORT}

CMD ${BUILD_COMMAND}
